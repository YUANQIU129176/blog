<% if (user.name) { %>
    <div id='menu'>
      <span class='name'><%= user.name %></span>
      <a href='/logout'>退出</a>
    </div>
  <% } else { %>
    <div id='menu'>
      <a href='/login'>登录</a>
      <a href='/register'>注册</a>
    </div>
  <% } %>
  